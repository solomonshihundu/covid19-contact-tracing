<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Widget.MaterialComponents.Button.TextButton" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="android:textColor">@color/black</item>
        <item name="android:paddingLeft">@dimen/mtrl_btn_text_btn_padding</item>
        <item name="android:paddingRight">@dimen/mtrl_btn_text_btn_padding</item>
        <item name="backgroundTint">@color/lightCoral</item>
        <item name="rippleColor">@color/lightCoral</item>
        <item name="strokeColor">@color/black</item>
        <item name="elevation">15dp</item>
        <item name="cornerRadius">8dp</item>
        <item name="strokeWidth">1dp</item>
    </style>

</resources>
